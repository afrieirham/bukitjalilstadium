# One Stadium, by design

The site documents exactly one Stadium, Bukit Jalil National Stadium, even though the site that inspired it hosts many venues. There is no venue dimension anywhere: no venue picker, no `/venue/:id` route, and no venue reference in any record. Adding a second Stadium later is a deliberate migration, not a configuration change.
